# crypto1_dlp

For using the testing script, run:

```bash
# Using virtual environment if needed
python -m venv venv && source venv/bin/activate

# Install dependencies
pip install requirements.txt
```